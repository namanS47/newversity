import 'package:flutter/material.dart';

class TeacherOnBoardingRoute extends StatelessWidget {
  const TeacherOnBoardingRoute({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
